const Loading = () => {
  return (
    <div className="h-screen w-full flex items-center justify-center">
      <div className="animate-spin w-12 h-12 border-4 border-[#ffcc00] border-t-transparent rounded-full"></div>
    </div>
  );
};

export default Loading;